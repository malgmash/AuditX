/** Product defaults from workstreams/03-employee.md. Flip here, not in the screens. */
export const SHOW_SCORE_NUMBER = true;
export const SHOW_DECLINED_FINDINGS = true;

/** Fields at or below this confidence are marked for the employee to check. */
export const LOW_CONFIDENCE_THRESHOLD = 0.8;

/** Receipt images larger than this are rejected before storage. */
export const MAX_RECEIPT_BYTES = 8 * 1024 * 1024;
